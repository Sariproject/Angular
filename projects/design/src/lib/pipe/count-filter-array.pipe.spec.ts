import { CountFilterArrayPipe } from './count-filter-array.pipe';

describe('CountFilterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CountFilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
