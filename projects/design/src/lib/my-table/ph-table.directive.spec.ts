import { PhTableDirective } from './ph-table.directive';

describe('PhTableDirective', () => {
  it('should create an intance', () => {
    const directive = new PhTableDirective();
    expect(directive).toBeTruthy();
  });
});
